---

---

<section class="hero" id="hero">
  <div class="hero-overlay"></div>
  <div class="hero-content">
    <h1>Wohnüberbauung mit 10 exklusiven Eigentumswohnungen in Lengnau</h1>
    <p>
      Exquisites Wohnen in zeitloser Eleganz - Luxuriöse Eigentumswohnungen in
      privilegierter Lage
    </p>
    <div class="hero-buttons">
      <a href="#apartments" class="btn primary">Wohnungen entdecken</a>
      <a href="#features" class="btn secondary">Ausstattung</a>
      <a href="#location" class="btn secondary">Lage</a>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    height: 90vh;
    min-height: 600px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: url("/images/hero-image.jpg");
    background-size: cover;
    background-position: center;
    color: white;
    text-align: center;
    overflow: hidden;
  }

  .hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.4));
    z-index: 1;
  }

  .hero-content {
    max-width: 900px;
    padding: 3rem;
    z-index: 2;
    position: relative;
  }

  .hero-content::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    border: 1px solid rgba(201, 166, 84, 0.3);
    transform: scale(1.05);
    z-index: -1;
  }

  h1 {
    font-family: "Playfair Display", serif;
    font-size: 3rem;
    margin-bottom: 1.5rem;
    line-height: 1.2;
    font-weight: 700;
    letter-spacing: 0.02em;
    text-shadow: 0px 2px 4px rgba(0, 0, 0, 0.3);
  }

  p {
    font-family: "Montserrat", sans-serif;
    font-size: 1.3rem;
    margin-bottom: 2.5rem;
    font-weight: 300;
    letter-spacing: 0.03em;
    line-height: 1.6;
    max-width: 80%;
    margin-left: auto;
    margin-right: auto;
  }

  .hero-buttons {
    display: flex;
    gap: 1.5rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-top: 1rem;
  }

  .btn {
    display: inline-block;
    padding: 1rem 2.5rem;
    border-radius: 0;
    text-decoration: none;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    font-size: 0.85rem;
    transition: all 0.4s ease;
    position: relative;
    overflow: hidden;
  }

  .primary {
    background-color: var(--gold);
    color: white;
    border: 1px solid var(--gold);
  }

  .primary:hover {
    background-color: transparent;
    color: var(--gold);
  }

  .secondary {
    background-color: transparent;
    color: white;
    border: 1px solid white;
  }

  .secondary:hover {
    background-color: white;
    color: var(--text-dark);
  }

  @media (max-width: 768px) {
    h1 {
      font-size: 2.2rem;
    }

    p {
      font-size: 1.1rem;
      max-width: 100%;
    }

    .hero-content {
      padding: 2rem;
    }
  }
</style>
